# Task 6: Docker & Compose: containerize app + redis + celery + minio + postgres, single-command up.

Repository branch: feature/06-docker-compose

Plan:
- Create models and schemas
- Implement routes and services
- Add tests and CI jobs

